services:
  - type: web
    name: ams-updater
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn web_app:app
    envVars:
      - key: FLASK_SECRET_KEY
        generateValue: true
